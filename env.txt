# =============================================================================
# Doctor — .env configuration
# Copy to .env and fill in your values. Never commit .env to git.
# =============================================================================

# ── Repo ─────────────────────────────────────────────────────────────────────
SERVICE=my-service
BRANCH=main
REPO=https://github.com/your-org/your-repo.git

# ── VCS Provider ──────────────────────────────────────────────────────────────
# Supported: github | gitlab | azure | bitbucket
VCS_PROVIDER=github

# Token for the VCS provider above (only one needed)
GITHUB_TOKEN=ghp_...
# GITLAB_TOKEN=glpat-...
# AZURE_TOKEN=...
# BITBUCKET_TOKEN=...

# Self-hosted GitLab or Azure base URL (leave blank for cloud)
# VCS_BASE_URL=https://git.your-company.com

# ── Monorepo ──────────────────────────────────────────────────────────────────
# Set to true if repo contains multiple services in subdirectories
# When true, declare services in config/repos.yaml
IS_MONOREPO=false

# ── Claude ───────────────────────────────────────────────────────────────────
# Leave empty to use Claude Code default model
CLAUDE_MODEL=

# Optional — enables token usage tracking after each run
# Get your key: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Number of endpoints to analyze in parallel within a run
PARALLEL_WORKERS=4

# ── PR ───────────────────────────────────────────────────────────────────────
# Set to true to automatically raise a PR after generation
# Requires VCS token with write access to the repo
RAISE_PR=false

# Branch to PR into (defaults to BRANCH above)
# PR_BASE_BRANCH=main

# ── Server mode only ──────────────────────────────────────────────────────────
# API key required to call the Doctor HTTP API
# DOCTOR_API_KEY=your-secret-api-key